#include <bits/stdc++.h>
#define int long long
using namespace std;
 
main() {
    int a, b, n, m;
    cin >> a >> b >> n >> m;
    for(int i=0; i < 1e6; i++){
        if((a +  n*i - b)% m == 0){
            cout << a +  n*i << endl;
            break;
        }
    }
    return 0;
}
